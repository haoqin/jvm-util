db {
    dataSourceClassName = org.h2.jdbcx.JdbcDataSource
    dataSource {
        url = "jdbc:h2:mem:PUBLIC;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;SCHEMA=PUBLIC;INIT=RUNSCRIPT from 'classpath:db/migration/V1__Create_DB.sql'"
        user = orca
    }
}